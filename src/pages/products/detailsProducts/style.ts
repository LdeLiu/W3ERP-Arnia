import { styled } from "styled-components";

export const StyledContent = styled.div`
    display: flex;
    flex-direction: column;
    gap: 1rem;

    padding: 1rem;

    color: #212121;
    .title{
        color: #001C98;
    }
`

export const StyledDetailsHeader = styled.div`
    display: flex;
    padding: 1rem;
    gap: 1rem;
`
export const StyledTablesDetails = styled.div`
    display: flex;
    gap: 1rem;
`
